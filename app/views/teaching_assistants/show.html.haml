%h3= @teaching_assistant.name

%h4
  Credit hour balance:
  = @teaching_assistant.balance

%span.badge{class: @teaching_assistant.status.label}
  = @teaching_assistant.status.label
= render partial: 'action_badge', locals: { teaching_assistant: @teaching_assistant }

.line__secondary

%h4 Upcoming Schedule

%table
  %tr
    %th Info
    %th Course & Location
    %th Date & Time
    %th
  - @teaching_assistant.schedule.each do |course|
    = render partial: 'sign_ups/course_row', locals: { course: course }

.line__secondary

%h4 History

= render partial: 'shared/ta_history_table', locals: { teaching_assistant: @teaching_assistant }

= render partial: 'shared/symbols'